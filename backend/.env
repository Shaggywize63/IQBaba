# Server Configuration
PORT=5001
BASE_PATH=
NODE_ENV=development

# Database Configuration (These should match the root .env)
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=
DB_NAME=olympiad_db

# Security
JWT_SECRET=your_super_secret_jwt_key_here